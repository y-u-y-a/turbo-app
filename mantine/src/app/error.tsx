"use client"

import { Center, Container, Title } from "@mantine/core"

export default function ServerErrorPage() {
  return (
    <Container py={40}>
      <Center>
        <Title size="h1">Occuer Server Error</Title>
      </Center>
    </Container>
  )
}
